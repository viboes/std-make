# To update the jasel version, from a *CLEAN* working directory, update the version numbers below.
# This makefile will generate a new version.hpp, *AMEND THE MOST RECENT COMMIT*, and git-tag the commit.
set(JASEL_MAJOR 0)
set(JASEL_MINOR 3)
set(JASEL_PATCHLEVEL 1)
